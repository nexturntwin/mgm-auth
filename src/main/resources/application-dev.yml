#These db properties are optional for H2 embedded database
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: murugan
    password: test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
      
server:
  port: 8005
  
megam:
  name: MEGAM
  email: teammegham@gmail.com
  security:
    salt: private
    encoder: pbkdf2
  
logging:
  level:
    '[org.springframework.security]': debug
    '[com.megam.security.mgmauth]': debug
    '[org.keycloak]': trace   

keycloak:
  auth-server-url: http://localhost:8300/auth
  realm: megamauth
  resource: megam-auth
  public-client: true
  principal-attribute: preferred_username
    
